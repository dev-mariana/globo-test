version: "3.8"

services:
  mongodb:
    image: mongo:latest
    container_name: feedback-platform
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root123
      - MONGO_INITDB_DATABASE=feedback-platform
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
